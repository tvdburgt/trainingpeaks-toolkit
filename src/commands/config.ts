export async function run(_argv: string[]): Promise<void> {
  console.log(`tp config — not yet implemented.

Planned scope:
  - Print resolved environment config (cookie present y/n, athlete id, workspace dir)
  - Show athlete profile summary from ATHLETE.md once that lands
  - Possibly: validate/refresh TP cookie
`);
  process.exit(2);
}
