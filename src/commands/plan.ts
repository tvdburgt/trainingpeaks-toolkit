export async function run(_argv: string[]): Promise<void> {
  console.log(`tp plan — not yet implemented.

Planned scope (see TODO.md section H):
  - Draft proposed training weeks into <workspace>/_generated/proposed/YYYY-Www.md
  - Use the athlete profile (ATHLETE.md) and recent load (CTL/ATL/TSB)
  - Flags likely: --week, --target-ctl, --a-race
`);
  process.exit(2);
}
